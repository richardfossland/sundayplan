import { PlaceholderPage } from "@/components/ui";

export default function Page() {
  return (
    <PlaceholderPage
      title="Songs"
      phase="Phase 3.2"
      blurb="A light song library — metadata and files, not slide content. Syncs setlists to SundayStage and carries CCLI + TONO identifiers for licensing reports."
    />
  );
}
