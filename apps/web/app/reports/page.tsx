import { PlaceholderPage } from "@/components/ui";

export default function Page() {
  return (
    <PlaceholderPage
      title="Reports"
      phase="Phase 11"
      blurb="Volunteer balance, service coverage, and licensing usage — producing both a CCLI report and a Norwegian TONO report from one shared usage log."
    />
  );
}
