import { PlaceholderPage } from "@/components/ui";

export default function Page() {
  return (
    <PlaceholderPage
      title="Services"
      phase="Phase 3.1"
      blurb="Reusable service templates and the order-of-service editor — drag-drop items, set required roles per section, and build the setlist."
    />
  );
}
